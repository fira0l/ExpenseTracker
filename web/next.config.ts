import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    remotePatterns: [
      { protocol: 'https', hostname: '*.supabase.co' },
    ],
  },
  // Ensure Vercel deploys correctly
  output: 'standalone',
};

export default nextConfig;
